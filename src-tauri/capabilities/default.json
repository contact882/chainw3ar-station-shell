{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core runtime access for the station windows",
  "windows": ["main", "sim"],
  "permissions": ["core:default"]
}
